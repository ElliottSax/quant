# Additional dependencies for stock market prediction features
# Add these to your main requirements.txt or install separately

# Market Data APIs
alpha_vantage>=2.3.1
twelvedata>=1.2.14
finnhub-python>=2.4.20

# Technical Analysis
pandas-ta>=0.3.14b  # 130+ indicators, pure Python
# TA-Lib: brew install ta-lib (Mac) or conda install -c conda-forge ta-lib
# Then: pip install TA-Lib

# Machine Learning (for future prediction models)
tensorflow>=2.15.0  # Or tensorflow-cpu for smaller install
torch>=2.2.0  # PyTorch for alternative models
xgboost>=2.0.3
lightgbm>=4.3.0
optuna>=3.5.0  # Hyperparameter optimization

# Backtesting
# DO NOT ADD vectorbt: its licence carries the Commons Clause, which forbids selling
# software whose value derives substantially from it — incompatible with a monetized
# site. It was never imported by app code; the dependency line is removed so it cannot
# arrive by accident. Same restriction applies to OpenBB (AGPL) and mlfinlab (proprietary).
# Licence-clean alternatives if a vectorized engine is needed: QuantConnect Lean
# (Apache-2.0), quantstats (Apache-2.0), or the existing in-house pandas engine.
# backtrader>=1.9.78   # GPL-3.0 — SaaS use only, never redistribute
# bt>=0.2.9

# Time Series & Statistics
statsmodels>=0.14.1  # Already in main requirements
prophet>=1.1.5  # Facebook Prophet for forecasting (optional)

# Additional utilities
joblib>=1.3.2  # Model persistence
