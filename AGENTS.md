# AGENTS.md

<!-- Auto-generated scaffold (@shared/jules/generate-agents-md.mjs, 2026-09-07).
     Detected from: no recognized manifest (package.json / requirements.txt / pyproject.toml / go.mod). VERIFY every command below actually works in this repo before
     relying on it -- this is a heuristic starting point for Jules (and any other coding agent that
     reads AGENTS.md), not a confirmed-accurate build doc. Edit freely; Jules re-reads this file on
     every session. -->

## Project
ElliottSax/quant (stack: unknown).

## Setup
- **Install:** _not detected -- fill in if one exists_

## Build
- **Build:** _not detected -- fill in if one exists_

## Test
- **Test:** _not detected -- fill in if one exists_

## Lint / typecheck
- **Lint:** _not detected -- fill in if one exists_

## Dev server
- **Dev:** _not detected -- fill in if one exists_

## Conventions
<!-- Add repo-specific rules here: branch naming, commit style, directories Jules should never
     touch (secrets, generated output, vendored code), and anything else a human contributor would
     need to be told once. Jules' own guidance is that SPECIFIC instructions here (not vague ones)
     are what makes short issue-label prompts reliable -- see the 2026-09-07 jules_github_integration
     feasibility note for the source of that claim. -->
